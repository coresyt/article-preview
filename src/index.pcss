@tailwind base;
@tailwind components;
@tailwind utilities;

@font-face {
  src: url('/font/Manrope-Bold.ttf');
  font-weight: bold;
  font-family: 'Manrope';
}

@font-face {
  src: url('/font/Manrope-Medium.ttf');
  font-weight: 500;
  font-family: 'Manrope';
}

@layer base {
  :root {
    --height-card: 515px;
    --width-card: 325px;
  }

  :root,
  html,
  body {
    font-size: 13px;
    box-sizing: border-box;
    overflow: hidden;
  }
}

@layer components {
  .attribution {
    font-size: 11px;
    text-align: center;
  }
  .attribution a {
    color: hsl(228, 45%, 44%);
  }
}

@media (768px <= width) {
  :root {
    --height-card: 280px;
    --width-card: 730px;
  }
}
