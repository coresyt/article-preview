@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root,
  body {
    font-size: 13px;
  }
}

@layer components {
  .attribution {
    font-size: 11px;
    text-align: center;
  }
  .attribution a {
    color: hsl(228, 45%, 44%);
  }
}
